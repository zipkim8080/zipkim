spring:
  datasource:
    url:
    username:
    password:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto:
    properties:
      hibernate:

server:
  env: blue
  port: 8080
  serverAddress: localhost

serverName: local_server