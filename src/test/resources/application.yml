spring:
  profiles:
    active: local
  datasource:
    url:
    username:
    password:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
    show-sql: true
#---
#spring:
#  config:
#    activate:
#      on-profile: local

server:
  env: blue
  port: 8080
  serverAddress: localhost

serverName: local_server

file:
  dir: ""

---
spring:
  data:
    redis:
      host: test-redis
      port: 6380